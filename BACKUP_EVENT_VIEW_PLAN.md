# 备份运行日志与事件时间线实施计划

## 背景

当前项目已经具备较完整的备份侧数据基础：

- `BackupRecord`：记录单次备份结果、状态、错误信息、时长等
- `BackupScheduleRun`：记录定时任务批次执行结果
- `TaskEvent`：记录任务开始、失败、重试、存储上传等细粒度事件

现阶段的主要缺口不是缺少备份执行数据，而是这些数据尚未形成统一、易检索、易排障的可视化入口。因此，后续更适合补齐“运行日志视图”和“事件时间线展示”，而不是新增一套重复的“备份日志表”。

## 实施目标

- [ ] 新增“备份运行日志/任务事件”页面或统一筛选视图
- [ ] 支持按 `record_id`、`run_id`、`task_id`、设备、事件类型、失败类型、时间范围进行筛选
- [ ] 在备份详情页展示任务事件时间线
- [ ] 将 `BackupRecord`、`BackupScheduleRun`、`TaskEvent` 关联展示，便于定位一次备份的完整生命周期
- [ ] 保持现有数据模型优先复用，避免新增重复存储

## 范围定义

本计划优先覆盖以下两项能力：

### 1. 备份运行日志/任务事件统一视图

目标是提供一个集中入口，用于查看和筛选备份执行过程中产生的任务事件，增强运维排障能力。

建议展示内容：

- 事件时间
- 事件类型
- 关联设备
- 关联备份记录 ID
- 关联计划运行 ID
- Celery Task ID
- 失败类型
- 存储类型
- 是否成功
- 扩展详情 `details`

建议筛选条件：

- 关键字搜索
- 事件类型
- 设备 ID / 设备名称
- `record_id`
- `run_id`
- `task_id`
- `failure_type`
- `storage_type`
- 成功 / 失败
- 时间范围

### 2. 备份详情页事件时间线

目标是在单条备份详情页中，按时间顺序展示该备份相关事件，帮助用户快速回答以下问题：

- 任务是否成功入队
- 任务是否真正开始执行
- 是否发生重试
- 失败发生在哪个阶段
- 备份成功后是否成功上传到 S3 / FTP

建议时间线节点包括：

- `backup_record_task_started`
- `backup_record_task_succeeded`
- `backup_record_task_failed`
- `backup_record_task_retry_scheduled`
- `backup_record_storage_upload`
- `backup_record_task_aborted`
- `backup_record_task_signal_failure`
- `backup_record_task_revoked`

## 实施步骤

### 第一阶段：梳理查询与关联关系

- [ ] 明确 `TaskEvent.record_id`、`TaskEvent.run_id`、`TaskEvent.task_id` 的使用边界
- [ ] 梳理 `BackupRecord` 与 `TaskEvent` 的可追踪链路
- [ ] 梳理 `BackupScheduleRun` 与 `BackupScheduleRunItem`、`BackupRecord` 的关联查询方式
- [ ] 评估现有索引是否满足列表页和详情页查询性能

### 第二阶段：新增运行日志查询服务

- [ ] 新增面向页面的运行日志查询服务，统一封装筛选、分页、排序逻辑
- [ ] 提供事件类型、失败类型、存储类型等枚举整理能力
- [ ] 输出适配前端表格展示的结构化数据
- [ ] 保持与现有 `crud.py`、服务层风格一致

### 第三阶段：新增运行日志页面或统一视图

- [ ] 增加新的路由入口与页面模板
- [ ] 支持分页、筛选、清空筛选、状态展示
- [ ] 在表格中展示事件摘要与关联对象信息
- [ ] 支持从备份记录、计划运行记录跳转进入对应筛选视图

### 第四阶段：在备份详情中增加事件时间线

- [ ] 为单条备份记录查询关联事件列表
- [ ] 按时间顺序构建时间线数据
- [ ] 为不同事件类型提供中文文案与视觉状态区分
- [ ] 在备份详情页补充“执行过程”或“事件时间线”区域
- [ ] 在失败场景中突出失败原因、失败类型、是否已重试、上传是否成功

### 第五阶段：测试与验收

- [ ] 为运行日志筛选与分页补充服务层测试
- [ ] 为备份详情时间线组装逻辑补充测试
- [ ] 覆盖“成功、失败、重试、上传失败、任务撤销”等关键场景
- [ ] 验证无事件数据、历史旧数据、字段缺失场景下的兼容性

## 设计原则

- 优先复用 `TaskEvent`，不新增重复的“备份日志表”
- 结果页与详情页的数据口径保持一致
- 事件展示以排障可读性优先，不暴露无意义内部字段
- 保持与现有“备份历史”“计划统计”“操作日志”“登录日志”页面的交互风格一致
- 兼顾后续扩展到导出、告警联动、外部日志平台对接的可能性

## 验收标准

- [ ] 用户可以在统一入口查看备份运行事件
- [ ] 用户可以通过 `record_id` 或设备快速定位某次备份相关事件
- [ ] 用户可以在备份详情页看到完整事件时间线
- [ ] 关键失败原因、重试行为、上传结果可以被直接识别
- [ ] 实现过程中不引入新的重复日志模型

## 后续可选增强

- [ ] 增加运行日志导出能力
- [ ] 增加按计划任务维度查看事件流的页面
- [ ] 增加从仪表盘失败任务跳转到事件视图的快捷入口
- [ ] 增加事件保留周期配置项，替代当前固定 90 天策略
