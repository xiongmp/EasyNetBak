# 多级分组与权限兼容改造实施计划

## 背景

当前项目中的设备分组能力是典型的“单层平铺模型”：

- `DeviceGroup` 只有 `id`、`name`、`created_at`
- `Device` 通过 `group_id` 直接关联一个分组
- `User` 通过 `group_access_type` + `allowed_group_ids` 控制可访问分组范围
- 权限校验、设备列表、备份历史、配置搜索、定时任务目标筛选，基本都依赖“设备所属分组 ID 是否命中允许列表”

现有实现已经比较稳定，因此本次改造的前提必须是：

- 现有设备已绑定的分组不能被破坏
- 现有用户权限不能因改造出现越权或误拒绝
- 现有接口、页面、定时任务配置尽量保持兼容

## 现状结论

结合当前代码实现，可以确认以下事实：

- 设备分组是单值归属，不是多对多关系，因此本次需求更适合做“多级分组树”，不建议同步演变成“设备属于多个分组”
- 现有设备兼容的关键点在于 `Device.group_id`，只要这个字段不被重写、不强制迁移，老设备就不会被影响
- 现有权限兼容的关键点在于 `User.allowed_group_ids`，只要保留旧存储方式并在运行时扩展子孙分组即可实现平滑升级
- 现有定时任务目标支持 `group:<id>`，也支持按分组名称解析；一旦出现层级分组，按名称解析会出现歧义，必须处理

## 可行性判断

结论：**可以实现，且可以在不破坏现有设备分组的前提下平滑演进为多级分组。**

推荐采用“**设备归属不变，分组结构升级，权限按分组树扩展**”的方案：

- 保留 `Device.group_id` 作为设备唯一归属分组
- 给 `DeviceGroup` 增加父子关系字段，形成树结构
- 让现有所有分组默认成为一级根分组
- 让用户权限仍然保存“被授权的分组 ID”，但在运行时自动展开其子孙节点

这样做的好处是：

- 旧设备不需要强制迁移
- 旧用户授权数据不需要立刻改表
- 改造可以按“先支持树结构，再切权限，再改前端”的顺序逐步发布

## 目标设计

### 一、分组模型升级

建议在 `DeviceGroup` 上新增以下字段：

- `parent_id`：父分组 ID，根分组为空
- `path`：祖先路径，建议保存为稳定的 ID 路径，如 `/1/5/18/`
- `depth`：层级深度，便于前端展示和快速排序
- `sort_order`：可选，便于后续树形展示排序

建议保留现有字段不变：

- `id`
- `name`
- `created_at`

建议约束：

- 禁止形成循环父子关系
- 禁止把自己挂到自己的子孙节点下
- 初期建议继续保持“分组名称全局唯一”

之所以建议先保持名称全局唯一，是因为当前定时任务解析逻辑支持通过名称映射分组；如果同层或跨层允许重名，旧逻辑会直接产生歧义。

### 二、设备归属策略

设备层建议维持当前模型：

- 一个设备只归属一个分组
- 仍然使用 `Device.group_id`
- 不引入 `device_group_rel` 这类多对多关系表

兼容策略：

- 现有设备继续指向原分组 ID
- 原有分组升级后默认都是根分组
- 后续如果业务需要“把某个原分组拆成多个子分组”，只新增子分组，不强制迁移所有老设备
- 设备迁移应由管理员按批次执行，而不是在数据库迁移时自动重分配

这意味着：

- 改造完成当天，老设备行为不变
- 多级分组能力主要先服务“新增结构”和“后续渐进梳理”

### 三、权限模型升级

推荐第一阶段不要立即推翻 `User.allowed_group_ids` 的存储方式，仍然保留逗号分隔的分组 ID 字符串。

但其语义要从“**精确命中的分组**”调整为“**授权根节点集合**”：

- 用户被授权某个父分组，则默认拥有该父分组及其全部子孙分组的访问权
- 用户被授权某个叶子分组，则只能访问该叶子分组
- `group_access_type == all` 的用户逻辑不变
- `-1` 或 `0` 代表未分组设备的兼容逻辑继续保留

推荐新增统一能力：

- `expand_group_ids(selected_ids)`：把已授权分组展开成“自身 + 子孙”的完整访问集合
- `build_group_subtree_condition(group_id)`：构造分组子树查询条件
- `build_allowed_group_condition(user)`：统一输出当前用户可访问设备的 SQL 条件

### 四、接口与页面策略

建议改造为“双轨兼容”：

- 现有“分组列表接口”继续保留，返回扁平结果，避免外部调用直接中断
- 新增“分组树接口”，供前端树形组件使用
- 现有设备编辑页面的分组选择改为树形选择器
- 用户授权页面的 `allowed_group_ids` 改为树形勾选
- 分组管理页从表格演进为树表或树列表

公开 API 建议：

- 保留原有 `/api/v1/groups`
- 新增 `/api/v1/groups/tree`
- 原有 `GroupResponseSchema` 可新增 `parent_id`、`path`、`depth` 等字段，但不要删除旧字段

## 推荐实施方案

### 方案总原则

采用“**先加结构，后扩权限，再改交互，最后做业务迁移**”的顺序，避免一步到位造成大面积回归。

### 第一阶段：数据库兼容升级

目标：

- 先让数据库支持树结构，但不改变现有业务语义

实施项：

- 为 `devicegroup` 新增 `parent_id`、`path`、`depth`、`sort_order`
- 为 `parent_id` 建索引
- 为 `path` 建索引或预留查询优化手段
- 为现有全部分组回填：
  - `parent_id = NULL`
  - `depth = 0`
  - `path = /{id}/`

阶段验收：

- 所有现有分组自动成为一级根节点
- 所有设备仍按原有分组正常可见、可改、可备份
- 不需要迁移任何设备归属

### 第二阶段：封装分组树服务

目标：

- 建立统一的树形分组读写能力，避免后续权限、设备、定时任务各自写一套逻辑

实施项：

- 新增分组树查询服务
- 新增按父节点查询子节点、按节点查询子树、按节点查询祖先链路的能力
- 新增创建子分组、移动分组、校验循环引用的服务方法
- 删除分组时增加对子树的校验

删除规则建议：

- 父分组下有子分组时不能直接删除
- 分组或其子树下仍有关联设备时不能删除
- 如后续允许“级联删除”，也必须默认关闭，仅提供显式二次确认入口

### 第三阶段：权限链路改造

目标：

- 把现有“精确分组权限”升级成“树形分组权限”

实施项：

- 在权限入口统一展开 `allowed_group_ids`
- 改造以下模块的分组访问判断：
  - 设备列表与设备详情
  - 设备新增、编辑、删除、批量导入
  - 备份历史与备份详情
  - 配置搜索
  - 定时任务目标预览与执行
  - 公开 API 的设备与备份查询
  - 需要按设备可见范围过滤的页面

建议实现方式：

- 所有设备访问校验不再直接比较 `device.group_id in allowed_group_ids`
- 改为比较 `device.group_id in expanded_allowed_group_ids`
- 所有按分组筛选设备的 SQL 条件也统一复用同一个 helper

这样可以最大限度避免“某些入口已经支持多级分组，某些入口仍按旧逻辑精确匹配”的权限裂缝。

### 第四阶段：定时任务与历史配置兼容处理

目标：

- 处理定时任务中对分组目标的引用兼容问题

风险来源：

- 当前任务目标支持 `group:<id>`，也能按分组名称解析
- 一旦引入层级分组，名称可能不再适合作为稳定标识

建议处理方式：

- UI 保存任务目标时统一只保存 `group:<id>`
- 执行时优先按 ID 解析
- 保留旧名称解析一段时间作为兼容逻辑
- 增加一次历史巡检，识别现有 `targets` 中是否存在按分组名称保存的旧配置
- 对旧配置做预转换或在启动时告警

如果允许后续分组改名，则更要避免继续依赖组名解析，否则定时任务会出现“分组改名后目标失效”的问题。

### 第五阶段：前端与管理交互升级

目标：

- 让管理员真正可视化管理树形分组和树形授权

实施项：

- 分组管理页改为树表
- 新增“新增子分组”“移动分组”操作
- 设备编辑页的分组选择改为树形下拉
- 用户管理页的分组授权改为树形勾选
- 授权界面增加“授权该节点即自动包含全部下级”的提示

交互建议：

- 初期先不做复杂的半选态持久化
- 初期只保存显式勾选的节点 ID
- 展示层可以有半选态，但存储层保持简单

### 第六阶段：业务侧渐进迁移

目标：

- 在不影响现网的前提下，逐步把旧的平铺分组梳理成树结构

建议做法：

- 先建立新的树形框架，不立即移动设备
- 选择少量业务域进行试点
- 每次迁移只处理一个根分组下的一批设备
- 迁移前导出设备清单、用户授权清单、定时任务引用清单
- 迁移后进行可见性和任务执行回归

## 权限改造细则

### 权限语义

改造后建议采用以下语义：

- `all`：可访问所有分组与未分组设备
- `specific + [父分组A]`：可访问 A 及其全部后代分组下设备
- `specific + [子分组B]`：仅可访问 B 子树下设备
- `specific + [-1]`：仅可访问未分组设备

### 授权边界

需要特别明确以下问题：

- 用户是否允许同时授权父节点和子节点：允许，但服务层应去重压缩
- 用户是否允许仅授权中间节点：允许
- 用户是否允许看到父节点本身挂载的设备：建议允许
- 父节点是否仍允许直接挂设备：**建议迁移期允许，稳定后再评估是否限制只能叶子节点挂设备**

“父节点允许直接挂设备”是本次兼容的关键之一。若一开始就强制“只有叶子节点可挂设备”，将直接影响现有设备归属。

### 建议增加的统一规则

- 保存用户权限前，自动去重并做合法性校验
- 如果同时选择了父节点和其子节点，可自动压缩为父节点
- 分组被移动父节点后，要重新计算相关用户的展开权限缓存
- 若后续做权限缓存，缓存必须在分组树变更后失效

## 主要风险点与规避措施

### 风险一：现有设备分组被误迁移，导致设备不可见或任务范围变化

风险说明：

- 如果在数据库迁移阶段就自动把旧设备分散到新子分组，最容易引发权限、备份、定时任务范围变化

规避方案：

- 严禁在 schema migration 中自动重写 `Device.group_id`
- 所有旧设备默认保持原分组不变
- 设备迁移只能通过业务操作显式执行
- 上线前先导出“设备 ID -> 原分组 ID”快照，作为回滚依据

### 风险二：权限放大，出现越权访问

风险说明：

- 父分组授权天然会覆盖子孙分组，如果处理不清晰，容易比原先开放更多数据

规避方案：

- 明确文档语义：父节点授权即包含后代
- 上线前生成“用户可访问设备数量前后对比报表”
- 对重点角色做人工验收
- 为关键页面补充自动化测试，验证同一用户在改造前后可见设备集合符合预期

### 风险三：权限遗漏，部分入口仍按旧逻辑精确匹配分组

风险说明：

- 当前分组权限散落在多个服务和路由中，如果只改一部分，系统会出现“列表能看，详情不能进”或“备份页看不到设备”的问题

规避方案：

- 把分组访问判断统一收敛到公共 helper
- 改造时按模块清单逐项勾检
- 用关键场景回归测试覆盖：
  - 设备列表
  - 设备详情
  - 备份历史
  - 配置搜索
  - 定时任务预览
  - API 查询

### 风险四：定时任务目标因分组重名或改名失效

风险说明：

- 旧逻辑允许按组名解析目标，层级分组下若组名不再全局唯一，会直接歧义

规避方案：

- 新版本统一只写入 `group:<id>`
- 旧目标配置先扫描再升级
- 对仍使用名称的配置输出告警日志
- 初期继续要求分组名称全局唯一，等定时任务配置完成清洗后再评估是否放开重名

### 风险五：分组移动或删除导致权限、设备、任务引用失真

风险说明：

- 父子层级一旦可调整，用户授权范围、任务目标和页面展示都会受影响

规避方案：

- 分组移动操作必须写审计日志
- 删除前检查：
  - 当前节点是否有子分组
  - 当前节点及子树下是否有设备
  - 是否被用户授权引用
  - 是否被定时任务目标引用
- 高风险操作增加二次确认

### 风险六：树形查询性能下降

风险说明：

- 设备列表、备份、搜索、统计等页面都可能频繁用到“某节点下全部后代分组”

规避方案：

- 初期优先采用 `path` + 服务层展开 ID 集合的方案
- 如果数据量继续增长，再评估：
  - 闭包表
  - 递归 CTE
  - 物化祖先链缓存
- 避免每个页面临时递归全表

### 风险七：外部 API 调用方兼容性受影响

风险说明：

- 若直接修改原接口结构，外部脚本可能失败

规避方案：

- 原扁平接口保留
- 新增树接口，不强推替换
- 原有字段继续输出
- 新字段只做增量扩展

## 受影响模块清单

本次改造至少需要评估和联动以下模块：

- 数据模型：`app/models.py`
- CRUD 与通用查询：`app/crud.py`
- 分组服务：`app/services/resource_service.py`
- 设备服务：`app/services/device_service.py`
- 备份服务：`app/services/backup_service.py`
- 定时任务服务：`app/services/schedule_service.py`
- 用户与角色服务：`app/services/identity_service.py`
- 权限入口：`app/routers/support.py`
- Web 页面路由：`app/routers/web/resources.py`、`app/routers/web/devices.py`、`app/routers/web/auth.py`
- API 路由：`app/routers/public_api/v1/groups.py`、`app/routers/public_api/v1/devices.py`、`app/routers/public_api/v1/backups.py`
- 调度执行：`app/scheduler.py`
- 页面模板：`app/templates/groups.html`、`app/templates/users.html`、`app/templates/devices.html`、`app/templates/schedules.html`

## 实施步骤

> 进度说明（2026-04-29）：
>
> - 已完成：分组树 Schema 升级、分组树公共 helper、树形权限展开主链、`/api/v1/groups/tree`、定时任务 `group:<id>` 规范化保存、旧 `group:组名` 兼容解析与启动告警、针对性自动化测试、前端分组管理树表、用户授权树形勾选、设备编辑树选择器、定时任务目标选择页树结构支持
> - 未完成：全量业务回归
> - 最新同步：已完成全部前端交互升级，等待全量业务回归与试点验收

### 第 0 步：上线前准备

- [ ] 备份数据库
- [ ] 导出当前设备与分组绑定关系
- [ ] 导出当前用户的分组授权关系
- [ ] 导出当前定时任务目标配置
- [ ] 识别是否存在按组名保存的任务目标

### 第 1 步：Schema 兼容升级

- [x] 为分组表新增树结构字段
- [x] 回填旧分组为根节点
- [x] 补充索引与基础约束
- [x] 确保升级后旧页面仍可正常读写分组

### 第 2 步：服务层树能力建设

- [x] 增加分组树查询与校验能力
- [x] 增加展开子树 ID 的公共 helper
- [x] 增加移动分组、校验循环引用、删除前校验

### 第 3 步：权限链路切换

- [x] 所有设备访问校验统一改为树形权限
- [x] 所有按用户可见范围过滤设备的查询改为复用统一 helper
- [x] 保持 `all` 与未分组兼容逻辑不变

### 第 4 步：任务与 API 兼容

- [x] 定时任务目标统一按 `group:<id>` 保存
- [x] 增加分组树 API
- [x] 原有扁平 API 保持兼容
- [x] 增加旧配置兼容解析和告警

### 第 5 步：前端交互升级

- [x] 分组页改树表
- [x] 用户授权页改树形勾选
- [x] 设备编辑页改树形分组选择
- [x] 定时任务目标选择页支持树结构

### 第 6 步：试点迁移与验收

- [ ] 选择单个业务域试点
- [ ] 对比迁移前后用户可见设备范围
- [ ] 验证备份、搜索、定时任务、API 查询
- [ ] 确认无异常后逐步扩展到全量

## 测试建议

建议补充以下测试场景：

- [ ] 旧数据升级后，原设备分组不变
- [x] 根分组授权用户可以访问其子分组设备
- [ ] 子分组授权用户不能反向访问父分组其他设备
- [ ] 未分组设备权限逻辑保持兼容
- [ ] 设备编辑时不能移动到无权限的目标分组
- [ ] 备份历史只展示授权子树下设备
- [ ] 配置搜索只命中授权子树下设备
- [ ] 定时任务目标预览能正确展开子树设备
- [ ] 删除含子分组或含设备的分组会被拒绝
- [ ] 分组移动后权限结果正确刷新
- [x] 原有 `/api/v1/groups` 调用不报错

## 验收标准

- [ ] 升级后原有设备仍在原分组下可正常访问
- [ ] 新增父子分组后，设备、备份、搜索、任务均可按子树正确过滤
- [ ] 用户授权支持父节点包含全部下级
- [ ] 不出现明显越权或误拒绝
- [ ] 原有主要 API 和页面保持可用
- [ ] 定时任务目标不因分组层级升级而失效

## 回滚策略

建议按“两阶段可回滚”设计：

- 第 1 阶段只上线数据库字段和读兼容逻辑，不启用树形权限切换
- 第 2 阶段再开启树形权限和前端树控件

回滚时：

- 若仅第一阶段异常，可回退代码，保留新增字段不启用
- 若第二阶段异常，可关闭树形权限开关，继续按旧精确匹配逻辑运行
- 由于设备归属未被自动迁移，回滚成本会显著降低

## 最终建议

本项目最适合采用的落地方案是：

- **数据层**：在 `DeviceGroup` 上增加树结构字段
- **兼容层**：保留 `Device.group_id` 与 `User.allowed_group_ids`
- **权限层**：把“精确分组授权”升级为“节点子树授权”
- **发布层**：分阶段灰度，不做一次性全量分组迁移

这条路径可以满足“升级为多级分组”“不影响原有设备已使用的分组”“权限同步改造”三个目标，且整体风险可控、回滚路径清晰，适合作为正式实施方案。
